# 🇦🇪 UAE7Guard - دليل النشر السريع

## 📱 إصلاح مشكلة "Network error" في التطبيق

تم إصلاح المشكلة! التطبيق الآن جاهز للنشر على **Vercel + Supabase**.

---

## 🚀 خطوات النشر (15 دقيقة)

### 1️⃣ إنشاء قاعدة بيانات Supabase

1. **افتح** [supabase.com](https://supabase.com)
2. **أنشئ** مشروع جديد: `uae7guard`
3. **اختر** المنطقة: `Singapore` (الأقرب للإمارات)
4. **انتظر** دقيقتين حتى يجهز المشروع

### 2️⃣ استيراد قاعدة البيانات

1. **افتح** SQL Editor في Supabase
2. **انسخ** محتوى ملف `supabase-schema.sql`
3. **الصق** في المحرر واضغط "Run"
4. **تأكد** من ظهور رسالة النجاح ✅

### 3️⃣ نسخ رابط قاعدة البيانات

1. **اذهب** إلى Settings → Database
2. **انسخ** Connection String (URI)
3. **استبدل** `[YOUR-PASSWORD]` بكلمة المرور الحقيقية
4. **احتفظ** بهذا الرابط - ستحتاجه لاحقاً

**مثال:**
```
postgresql://postgres:كلمة_المرور@db.xxx.supabase.co:5432/postgres
```

---

### 4️⃣ النشر على Vercel

1. **افتح** [vercel.com/dashboard](https://vercel.com/dashboard)
2. **اضغط** "Add New" → "Project"
3. **اختر** مستودع GitHub: `UAE7Guard`
4. **اضغط** "Import"

### 5️⃣ إضافة متغيرات البيئة

**اضغط** "Environment Variables" وأضف:

| الاسم | القيمة |
|------|--------|
| `NODE_ENV` | `production` |
| `DATABASE_URL` | رابط Supabase من الخطوة 3 |
| `SESSION_SECRET` | سر عشوائي (32+ حرف) |
| `APPLE_REVIEW_PASSWORD` | `AppleReview2026` |

**لإنشاء SESSION_SECRET:**
```bash
openssl rand -base64 32
```

### 6️⃣ النشر

1. **اضغط** "Deploy"
2. **انتظر** 2-3 دقائق
3. **احصل** على الرابط: `https://uae7guard.vercel.app`
4. **احتفظ** بهذا الرابط!

---

### 7️⃣ تحديث التطبيق

**افتح** ملف `client/src/lib/api-config.ts`:

```typescript
// غيّر هذا السطر:
const PRODUCTION_API_URL = import.meta.env.VITE_API_BASE_URL || '';

// إلى رابط Vercel الخاص بك:
const PRODUCTION_API_URL = import.meta.env.VITE_API_BASE_URL || 'https://uae7guard.vercel.app';
```

**ثم قم بإعادة البناء:**
```bash
npm run build
npx cap sync
```

---

## ✅ اختبار التطبيق

### تجربة تسجيل الدخول:

- **البريد**: `applereview@uae7guard.com`
- **كلمة المرور**: `AppleReview2026`

**يجب أن يعمل بدون أخطاء!** ✅

---

## 📊 التكلفة

| الخدمة | الباقة المجانية |
|--------|-----------------|
| Vercel | 100GB نقل بيانات |
| Supabase | 500MB قاعدة بيانات |
| **المجموع** | **0 دولار/شهرياً** 🎉 |

---

## 🔧 حل المشاكل

### مشكلة: "Cannot connect to server"

**الحل:**
1. تحقق من نشر Vercel (يجب أن يكون أخضر ✅)
2. افتح `https://your-app.vercel.app/api/health`
3. يجب أن ترى: `{"status":"ok"}`

### مشكلة: "Database connection failed"

**الحل:**
1. تحقق من `DATABASE_URL` في Vercel
2. تأكد من كلمة المرور الصحيحة
3. جرب الاتصال من Supabase SQL Editor

---

## 📚 المزيد من التفاصيل

لمزيد من المعلومات، راجع:
- 📖 `DEPLOYMENT_GUIDE.md` (دليل مفصل بالإنجليزية)
- 🔐 `.env.example` (جميع متغيرات البيئة المتاحة)

---

## 🎯 قائمة التحقق

- [ ] Supabase: قاعدة البيانات جاهزة
- [ ] Vercel: النشر ناجح
- [ ] Environment Variables: تم إضافتها
- [ ] API Health: يعمل بنجاح
- [ ] Mobile App: تم تحديث الرابط
- [ ] Login: يعمل بدون أخطاء
- [ ] iOS/Android: تم الاختبار

---

**🎉 مبروك! تطبيقك الآن على Vercel + Supabase**

لا مزيد من أخطاء "Network error"! ✅
