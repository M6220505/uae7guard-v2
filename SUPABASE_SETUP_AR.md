# دليل إعداد قاعدة بيانات Supabase لـ UAE7Guard

هذا الدليل يشرح كيفية إعداد قاعدة بيانات Supabase لمشروع UAE7Guard بشكل كامل.

## 📋 الخطوات الأساسية

### 1️⃣ إنشاء مشروع Supabase جديد

1. **الذهاب إلى موقع Supabase:**
   ```
   https://supabase.com
   ```

2. **إنشاء حساب أو تسجيل الدخول**

3. **إنشاء مشروع جديد:**
   - اضغط على "New Project"
   - اختر اسم المشروع: `uae7guard`
   - اختر كلمة مرور قوية لقاعدة البيانات (احفظها!)
   - اختر المنطقة: `Middle East (Bahrain)` أو أقرب منطقة
   - اضغط "Create new project"

4. **انتظر 2-3 دقائق** حتى يتم إنشاء المشروع

### 2️⃣ الحصول على رابط الاتصال بقاعدة البيانات

1. **في لوحة تحكم Supabase:**
   - اذهب إلى: `Settings` → `Database`
   - انزل للأسفل حتى تجد `Connection String`

2. **اختر نوع الاتصال:**
   - انقر على التبويب `URI`
   - ستجد رابط مثل هذا:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxx.supabase.co:5432/postgres
   ```

3. **استبدل كلمة المرور:**
   - استبدل `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات التي أنشأتها

4. **انسخ الرابط الكامل** (سنحتاجه في الخطوة التالية)

### 3️⃣ تكوين ملف البيئة (.env)

1. **افتح ملف `.env` في مشروعك:**
   ```bash
   nano .env
   # أو استخدم محرر النصوص المفضل لديك
   ```

2. **حدّث متغير DATABASE_URL:**
   ```bash
   DATABASE_URL="postgresql://postgres:your_password@db.xxxxxx.supabase.co:5432/postgres"
   ```

3. **احفظ الملف**

### 4️⃣ تشغيل سكريبت إعداد قاعدة البيانات

الآن جاهز لإنشاء جميع الجداول والبيانات التجريبية!

#### الخيار الأول: إعداد كامل (موصى به)

```bash
npm run db:setup
```

هذا الأمر سيقوم بـ:
- ✅ إنشاء 14 جدول في قاعدة البيانات
- ✅ إضافة الفهارس للأداء الأمثل
- ✅ إعداد Triggers التلقائية
- ✅ إنشاء حسابات تجريبية للاختبار
- ✅ إضافة بيانات عينة (15 عنوان احتيال معروف)
- ✅ تكوين منتجات Stripe (اختياري)

#### الخيار الثاني: إعادة ضبط كاملة (⚠️ يحذف جميع البيانات)

```bash
npm run db:setup:force
```

**تحذير:** هذا الأمر سيحذف جميع الجداول والبيانات الموجودة ثم يعيد إنشاءها!

#### الخيار الثالث: إعداد مخصص

```bash
# بدون بيانات تجريبية
npm run db:setup -- --skip-seed

# بدون إعداد Stripe
npm run db:setup -- --skip-stripe

# إعادة ضبط بدون بيانات تجريبية
npm run db:setup -- --force --skip-seed
```

### 5️⃣ التحقق من نجاح الإعداد

بعد تشغيل الأمر، يجب أن ترى:

```
✓ Database connection established
✓ Database tables created successfully
✓ 14 tables verified
✓ Demo data seeded successfully

====================================================
DATABASE SETUP COMPLETE
====================================================

📊 Database Statistics:
   Tables created: 14

🔑 Demo Accounts:
   Admin User:
     Email: admin@uae7guard.com
     Password: admin123456

   Apple Review Account:
     Email: applereview@uae7guard.com
     Password: AppleReview2024!
```

## 📊 الجداول التي تم إنشاؤها

سيتم إنشاء 14 جدول في قاعدة البيانات:

| الجدول | الوصف |
|--------|-------|
| `sessions` | إدارة جلسات المستخدمين |
| `users` | حسابات المستخدمين والاشتراكات |
| `user_reputation` | نقاط الثقة ورتب المحققين |
| `scam_reports` | تقارير الاحتيال على البلوكشين |
| `alerts` | الإشعارات والتنبيهات |
| `watchlist` | العناوين المراقبة |
| `security_logs` | سجل التدقيق الأمني |
| `live_monitoring` | المراقبة الحية للمحافظ |
| `monitoring_alerts` | تنبيهات المعاملات |
| `escrow_transactions` | معاملات القفل الذكي |
| `slippage_calculations` | تحليل انزلاق الأسعار |
| `conversations` | محادثات الذكاء الاصطناعي |
| `messages` | سجل الرسائل |
| `encrypted_audit_logs` | سجلات تدقيق مشفرة قانونية |

## 🔐 الحسابات التجريبية

### حساب المدير (Admin)
```
البريد الإلكتروني: admin@uae7guard.com
كلمة المرور: admin123456
```

### حساب مراجعة Apple
```
البريد الإلكتروني: applereview@uae7guard.com
كلمة المرور: AppleReview2024!
```

## 📦 البيانات العينة المضافة

عند تشغيل الإعداد الكامل، سيتم إضافة:

- **7 حسابات محققين** بدرجات ثقة مختلفة
- **15 عنوان احتيال معروف** على Ethereum و Bitcoin
- **إدخالات قائمة المراقبة** للاختبار
- **تنبيهات وإشعارات** عينة

## 🚀 التشغيل بعد الإعداد

### 1. تشغيل خادم التطوير

```bash
npm run dev
```

### 2. فتح التطبيق في المتصفح

```
http://localhost:5000
```

### 3. تسجيل الدخول

استخدم أحد الحسابات التجريبية أعلاه

### 4. اختبار الميزات الأساسية

- البحث عن عناوين الاحتيال
- مراقبة المحفظة
- المساعد الذكي
- إدارة الاشتراكات

## 🔧 استكشاف الأخطاء وإصلاحها

### خطأ في الاتصال بقاعدة البيانات

```
Failed to connect to database
```

**الحلول:**
1. تحقق من صحة `DATABASE_URL` في ملف `.env`
2. تأكد من استبدال `[YOUR-PASSWORD]` بكلمة المرور الفعلية
3. تحقق من اتصالك بالإنترنت
4. تأكد من أن مشروع Supabase نشط

### أخطاء "already exists"

هذه أخطاء طبيعية إذا كانت الجداول موجودة مسبقاً. السكريبت سيتخطى الجداول الموجودة إلا إذا استخدمت `--force`.

### تخطي إعداد Stripe

إذا رأيت:
```
⚠ Skipping Stripe setup: STRIPE_SECRET_KEY not configured
```

هذا يعني أن `STRIPE_SECRET_KEY` غير موجود في `.env`. يمكنك إضافته لاحقاً.

### فشل تشغيل السكريبت

**الحل:**
```bash
# تحقق من تثبيت المكتبات
npm install

# حاول مرة أخرى
npm run db:setup
```

## 🌐 الإعداد للإنتاج (Production)

### في Vercel

1. **اذهب إلى لوحة تحكم Vercel:**
   - اختر مشروعك
   - اذهب إلى `Settings` → `Environment Variables`

2. **أضف متغيرات البيئة:**
   ```
   DATABASE_URL = postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
   SESSION_SECRET = [استخدم openssl rand -base64 32]
   NODE_ENV = production
   ```

3. **أعد نشر المشروع:**
   ```bash
   vercel --prod
   ```

4. **شغل إعداد قاعدة البيانات:**
   - من جهازك المحلي (مع DATABASE_URL للإنتاج)
   - أو استخدم Vercel CLI:
   ```bash
   vercel env pull .env.production
   DATABASE_URL=<production-url> npm run db:setup
   ```

## 📱 للتطبيقات المحمولة

بعد إعداد قاعدة البيانات، قم بمزامنة التطبيقات:

```bash
# iOS و Android
npm run cap:sync

# iOS فقط
npm run cap:sync:ios

# Android فقط
npm run cap:sync:android
```

## 🔄 عمليات قاعدة البيانات المتقدمة

### نسخ احتياطي لقاعدة البيانات

```bash
./scripts/backup-database.sh
```

### استعادة من نسخة احتياطية

```bash
./scripts/restore-database.sh path/to/backup.sql.gz
```

### إضافة بيانات تجريبية فقط (بدون تعديل الجداول)

```bash
npm run db:seed
```

### دفع تغييرات المخطط (Schema)

```bash
npm run db:push
```

## 📚 ملفات قاعدة البيانات المهمة

- **`supabase-schema.sql`** - مخطط SQL الكامل مع جميع الجداول
- **`server/seed.ts`** - منطق إضافة البيانات التجريبية
- **`server/seed-products.ts`** - إنشاء منتجات Stripe
- **`shared/schema.ts`** - تعريفات مخطط Drizzle ORM
- **`scripts/setup-database.ts`** - سكريبت الإعداد الآلي

## ✅ قائمة التحقق النهائية

- [ ] تم إنشاء مشروع Supabase
- [ ] تم الحصول على رابط الاتصال
- [ ] تم تحديث `.env` بـ DATABASE_URL
- [ ] تم تشغيل `npm run db:setup` بنجاح
- [ ] تم التحقق من إنشاء 14 جدول
- [ ] تم اختبار تسجيل الدخول بحساب المدير
- [ ] التطبيق يعمل على `http://localhost:5000`

## 🆘 الدعم

إذا واجهت أي مشاكل:

1. **راجع السجلات (Logs):**
   ```bash
   tail -f ./logs/app.log
   ```

2. **تحقق من وحدة التحكم في Supabase:**
   - اذهب إلى `Database` → `Tables`
   - تحقق من وجود جميع الجداول

3. **أعد المحاولة بالإعداد الكامل:**
   ```bash
   npm run db:setup:force
   ```

4. **تواصل مع الدعم:**
   - البريد الإلكتروني: support@uae7guard.com
   - GitHub Issues: https://github.com/yourusername/UAE7Guard/issues

## 🎉 تهانينا!

قاعدة بيانات Supabase الخاصة بك جاهزة الآن! يمكنك البدء في استخدام جميع ميزات UAE7Guard.
