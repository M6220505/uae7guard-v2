# دليل حل مشاكل VPN - تطبيق UAE7Guard

## المشكلة
رسالة خطأ: "Login failed - Network error. Please check your internet connection"

## السبب المحتمل: VPN
نعم، VPN يمكن أن يسبب هذه المشكلة. إليك الأسباب والحلول:

---

## 🔍 لماذا VPN يسبب المشكلة؟

### 1. تغيير IP Address المستمر
- التطبيق يتصل بـ: `https://uae7guard-m6220505.repl.co`
- بعض VPN تغير IP باستمرار مما يقطع الاتصال
- Rate limiting قد يحجب IP الجديد

### 2. مشاكل شهادات الأمان (SSL/HTTPS)
- التطبيق يستخدم HTTPS فقط للأمان
- بعض VPN تستخدم Man-in-the-Middle للتشفير
- قد يفشل التحقق من SSL Certificate

### 3. مشاكل Cookies والجلسات
- التطبيق يعتمد على Session Cookies
- بعض VPN تحجب أو تعدل الـ Cookies
- يسبب فشل في حفظ الجلسة

### 4. بطء الاتصال والـ Timeout
- التطبيق عنده timeout: 30 ثانية
- VPN بطيء = تأخير في الاستجابة
- النتيجة: Request Timeout Error

### 5. مشاكل DNS Resolution
- VPN يغير DNS servers
- قد يفشل في الوصول لـ `*.repl.co` domains
- أو يعطي IP خاطئ

### 6. حجب البورتات (Ports)
- التطبيق يستخدم HTTPS (port 443)
- بعض VPN أو Firewalls تحجب بورتات معينة
- خصوصاً في الشبكات المقيدة

---

## ✅ الحلول خطوة بخطوة

### الحل 1: التجربة بدون VPN (الأهم)
```bash
الخطوات:
1. ✓ أوقف VPN تماماً
2. ✓ افتح إعدادات الشبكة
3. ✓ تأكد من اتصال WiFi/Mobile Data شغال
4. ✓ جرب تسجيل الدخول مرة أخرى
5. ✓ إذا نجح = المشكلة من VPN 100%
```

### الحل 2: استخدام VPN موثوق
إذا كنت **محتاج** VPN، استخدم واحد من هذه:
- ✅ ExpressVPN (الأفضل)
- ✅ NordVPN
- ✅ ProtonVPN
- ✅ Surfshark
- ❌ تجنب VPN مجاني (يسبب مشاكل كثيرة)

### الحل 3: إعدادات VPN الصحيحة

#### أ. تغيير السيرفر
```
1. افتح تطبيق VPN
2. اختر سيرفر في بلد قريب (UAE, Saudi Arabia, Bahrain)
3. سيرفر قريب = سرعة أفضل = timeout أقل
```

#### ب. تغيير VPN Protocol
```
الـ Protocols المتوفرة (من الأفضل للأسوأ):
1. WireGuard (الأسرع والأحدث) ✅ جرب هذا أولاً
2. OpenVPN UDP (سريع ومستقر) ✅
3. OpenVPN TCP (أبطأ لكن أكثر استقرار) ✅
4. IKEv2/IPSec (قد يسبب مشاكل) ⚠️
5. L2TP/IPSec (قديم وبطيء) ❌

الخطوات:
1. إعدادات VPN → Protocol
2. اختر WireGuard أو OpenVPN UDP
3. أعد الاتصال
```

#### ج. Split Tunneling (استثناء التطبيق)
```
ميزة Split Tunneling = تخلي التطبيق يتصل بدون VPN

الخطوات:
1. إعدادات VPN → Split Tunneling
2. فعّل "Exclude Apps"
3. أضف "UAE7Guard" للقائمة
4. الآن التطبيق يتصل مباشرة (بدون VPN)
```

#### د. DNS Settings
```
بعض VPN عندها DNS بطيء أو محجوب

الخطوات:
1. إعدادات VPN → DNS
2. استخدم Custom DNS:
   - Google DNS: 8.8.8.8, 8.8.4.4
   - Cloudflare: 1.1.1.1, 1.0.0.1
3. احفظ وأعد الاتصال
```

### الحل 4: تحقق من إعدادات الأمان

#### على Android:
```
الإعدادات → الأمان والخصوصية → إعدادات متقدمة

تأكد من:
✅ "Trust user certificates" مفعّل
✅ "Unknown sources" مسموح (إذا من خارج Play Store)
✅ لا توجد قيود على Network Security
```

#### Certificate Pinning:
```
إذا VPN يستخدم Certificate الخاص فيه:
1. قد يفشل التحقق من شهادة uae7guard-m6220505.repl.co
2. جرب VPN Protocol مختلف
3. أو استخدم Split Tunneling
```

### الحل 5: تشخيص المشكلة

#### اختبار السرعة:
```bash
1. افتح متصفح على الجوال
2. اذهب إلى: https://uae7guard-m6220505.repl.co
3. إذا فتح = Backend شغال
4. إذا ما فتح = مشكلة VPN أو حجب

إذا ظهر error في المتصفح:
- "This site can't be reached" = DNS مشكلة
- "Connection timed out" = VPN بطيء أو محجوب
- "SSL Certificate error" = VPN يتدخل في HTTPS
```

#### اختبار بدون HTTPS:
```
لا تجرب HTTP (مو مسموح في التطبيق)
التطبيق يستخدم HTTPS فقط للأمان
```

### الحل 6: تحقق من Rate Limiting
```
إذا جربت تسجيل الدخول كثير:
- الخادم يحجبك لمدة 15 دقيقة
- Max: 5 محاولات خلال 15 دقيقة
- انتظر 15-20 دقيقة وجرب مرة ثانية
```

---

## 🔧 حلول تقنية متقدمة

### للمطورين: اختبار الاتصال

#### 1. تحقق من Network Connectivity:
```javascript
// التطبيق عنده Network Monitoring مدمج
// شوف أيقونة WiFi في أعلى الشاشة
// إذا حمراء = No Connection
// إذا خضراء = Connected
```

#### 2. فحص الـ Logs (Android):
```bash
# استخدم Android Logcat
adb logcat | grep -i "UAE7Guard\|network\|ssl\|vpn"

# شوف Errors مثل:
# - "SSL handshake failed"
# - "Connection timeout"
# - "Unable to resolve host"
```

#### 3. Test Backend Connectivity:
```bash
# من الجوال (باستخدام Termux):
curl -I https://uae7guard-m6220505.repl.co/api/health

# Expected Response:
# HTTP/2 200
# content-type: application/json

# إذا فشل = VPN أو Network مشكلة
```

---

## 📊 جدول التشخيص السريع

| العرض (Symptom) | السبب المحتمل | الحل |
|----------------|---------------|------|
| "Network error" فوراً | VPN يحجب Domain | أوقف VPN أو Split Tunneling |
| "Network error" بعد 30 ثانية | Timeout بسبب VPN بطيء | غيّر VPN Server أو Protocol |
| أحياناً يشتغل، أحياناً لا | VPN غير مستقر | استخدم VPN موثوق |
| يشتغل على WiFi، مو على Mobile Data | ISP يحجب | استخدم VPN موثوق |
| "SSL Certificate error" | VPN يتدخل في HTTPS | غيّر Protocol أو Split Tunneling |
| يشتغل بدون VPN فقط | VPN السبب 100% | استخدم Split Tunneling |

---

## 🎯 الحل الموصى به (Best Practice)

### الترتيب الموصى به للحلول:

1. **جرب بدون VPN أولاً** ✅
   - إذا نجح = المشكلة من VPN
   - انتقل للخطوة 2

2. **استخدم Split Tunneling** ✅
   - استثني UAE7Guard من VPN
   - تحتفظ بالـ VPN للتطبيقات الثانية

3. **استخدم VPN موثوق مع WireGuard** ✅
   - ExpressVPN أو NordVPN
   - Protocol: WireGuard
   - Server: قريب من موقعك

4. **غيّر DNS إلى Cloudflare** ✅
   - 1.1.1.1 (Primary)
   - 1.0.0.1 (Secondary)

5. **تحقق من إعدادات الأمان** ✅
   - تأكد مافي Certificate errors
   - تأكد Cookies مسموحة

---

## 🆘 إذا ما زالت المشكلة موجودة

### اتصل بالدعم الفني مع هذه المعلومات:

```
1. نوع الجهاز: Android / iOS
2. إصدار النظام: (مثال: Android 13)
3. نوع VPN: (مثال: NordVPN)
4. VPN Protocol: (مثال: WireGuard)
5. رسالة الخطأ بالضبط: "Network error..."
6. هل يشتغل بدون VPN: نعم / لا
7. Screenshots للـ Error
```

### تواصل:
- Email: support@uae7guard.com
- أو راسل من خلال التطبيق: Contact → Report Issue

---

## 📝 ملخص سريع

```
✅ المشكلة: VPN يسبب Network Error
✅ السبب: SSL, DNS, Timeout, IP changing
✅ الحل السريع: أوقف VPN وجرب
✅ الحل الدائم: Split Tunneling أو VPN موثوق
✅ إذا ما نفع: تواصل مع الدعم الفني
```

---

**آخر تحديث:** 2026-01-28
**الإصدار:** 1.0.0
**Platform:** Android/iOS
**Backend:** https://uae7guard-m6220505.repl.co
